# Cornet shell

This directory contains the visual shell pieces extracted from `AppShell`.

`AppShell` remains the orchestrator; these components should stay focused on one responsibility and should not access Supabase directly.
